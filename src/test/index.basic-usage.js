'use strict';

import {expect} from 'mai-chai';

describe ('Require components', () => {
  describe ('list()', () => {
    it ('produces the expected list of components', () => {
      expect (true).to.be.true ();
    });
  });
});
